// Mock for bun:test when running in vitest
// Re-exports vitest globals as bun:test equivalents
export { describe, it, test, expect, beforeEach, afterEach, beforeAll, afterAll, vi, mock } from "vitest";
